{
  "name": "raven",
  "version": "0.1.0",
  "private": true,
  "description": "Raven monorepo containing the API and web app.",
  "scripts": {
    "dev": "concurrently \"npm -C services/api run dev\" \"npm -C apps/web run dev\"",
    "dev:api": "npm -C services/api run dev",
    "dev:web": "npm -C apps/web run dev",
    "build": "npm -C services/api run build && npm -C apps/web run build",
    "lint": "echo 'Lint tasks can be added per workspace'",
    "update-sitemap": "bash scripts/update-sitemap.sh",
    "verify": "bash scripts/verify-setup.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.17"
  }
}
